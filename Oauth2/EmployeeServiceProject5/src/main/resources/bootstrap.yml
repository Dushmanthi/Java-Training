server:
  port: 8181
  eureka:
    client:
      register-with-eureka: true
      fetch-registry:
spring:
  application:
    name: employee
security:
    oauth2:
      token-info-uri: https://localhost:8282/oauth/check_token
client:
      client-id: dushi
      secret-id: dushipass
